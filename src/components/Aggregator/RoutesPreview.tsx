import { Box, Flex, Heading, Link, Text } from '@chakra-ui/react';
import styled from 'styled-components';

const RoutesPreview = () => {
	return (
		<Flex style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', width: '100%', height: '100%' }}>
			<p>Detailed stablecoin stability and risk metrics will be available here.</p>
		</Flex>
	);
};

export default RoutesPreview;
